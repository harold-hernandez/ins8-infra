# Copy to terraform.tfvars and fill in. terraform.tfvars is gitignored.
# The defaults in variables.tf already match this project's actual setup
# (project_id "ins8-frontend", repo "harold-hernandez/ins8-frontend") — you
# likely don't need to override anything here for a first apply.

# project_id = "ins8-frontend"
# github_repository = "harold-hernandez/ins8-frontend"

# Leave as the placeholder default until CI has pushed a real image at
# least once (see ../../README.md for the bootstrap order).
# frontend_image = "us-east4-docker.pkg.dev/ins8-frontend/app/frontend:latest"
